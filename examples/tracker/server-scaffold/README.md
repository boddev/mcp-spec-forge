# tracker-mcp

MCP server designed from an eval set of 6 questions over 7 documented API endpoint(s).

Scaffold generated by mcp-spec-forge. Implement each tool handler in `src/tools/` by calling the documented underlying API endpoints. Auth: `bearer_token` via env `API_TOKEN`.
