# MCP Server Design — tracker-mcp

> MCP server designed from an eval set of 6 questions over 7 documented API endpoint(s).

Generated by **mcp-spec-forge** (deterministic mode) on 2026-06-22T19:41:04.416Z.
Built from **6** eval questions over **7** documented endpoints.

## Server

- **Name:** tracker-mcp
- **Transport:** stdio
- **Auth:** bearer_token (env: `API_TOKEN`)

## Coverage summary

- **Questions covered:** 6/6 (100%)

## Tools

The forge produced **3** task-oriented tools. Each wraps one or more API
endpoints so a question is answered in a single tool call rather than many round-trips.

### `summarize_issue_repo`

Task-oriented tool for issue, repo, across, comment. Wraps 3 API endpoint(s), collapsing a list-then-detail (N+1) pattern into a single call so a question is answered without multiple round-trips. Example question it answers: "What open issues are assigned to Sarah across the backend repo?".

- **Entities:** issue, repo, across, comment
- **Covers questions:** q1, q2, q3, q6

**Underlying API calls (in order):**

1. `GET /search/issues` — Search issues across repositories
2. `GET /repos/{owner}/{repo}/issues/{number}/comments` — List comments on an issue
3. `GET /repos/{owner}/{repo}/issues/{number}` — Get a single issue by number _(foreach `searchIssues.items`, if `include_details == true`, concurrency 5, max 50)_

**Input schema:** `object{ owner, repo, number, q, max_items, include_details }`

**Output schema:** `object{ items, count, truncated }`

### `list_project_task`

Task-oriented tool for project, task. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "List all active projects and their tasks".

- **Entities:** project, task
- **Covers questions:** q4

**Underlying API calls (in order):**

1. `GET /projects/{projectId}/tasks` — List tasks for a project

**Input schema:** `object{ projectId, max_items }`

**Output schema:** `object{ items, count, truncated }`

### `list_project`

Task-oriented tool for project. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "Who is the owner of the billing project?".

- **Entities:** project
- **Covers questions:** q5

**Underlying API calls (in order):**

1. `GET /projects` — List all projects

**Input schema:** `object{ status, max_items }`

**Output schema:** `object{ items, count, truncated }`

## Resources

- **api_capability_index** — Normalized index of the 7 documented API endpoints (method, path, params, response fields) the tools are built from.
- **eval_coverage_matrix** — Maps each evaluation question to the tool that answers it, with confidence and gaps.

## Prompts

- **answer_with_api_context** — System/template prompt instructing the model to select the single best task-oriented tool, pass minimal inputs, and avoid issuing redundant multi-call sequences.
- **route_question_to_tool** — Few-shot template mapping representative eval questions to the tool that should handle them.

## Eval coverage matrix

| Question | Intent / prompt | Covered by | Confidence | Notes |
|---|---|---|---|---|
| q1 | What open issues are assigned to Sarah across the backend repo? | summarize_issue_repo | 0.22 | list-then-detail collapsed |
| q2 | Show me the details and all comments for each open issue in the backend repo | summarize_issue_repo | 0.36 | list-then-detail collapsed |
| q3 | Find issues mentioning authentication bugs | summarize_issue_repo | 0.24 |  |
| q4 | List all active projects and their tasks | list_project_task | 0.70 |  |
| q5 | Who is the owner of the billing project? | list_project | 0.28 |  |
| q6 | What is the status of issue number 42 in the backend repo? | summarize_issue_repo | 0.48 |  |

## Review checklist

- [ ] Every uncovered question has a planned tool or is out of scope.
- [ ] Broad/god tools are split or justified.
- [ ] HTML-derived endpoints verified against real API behavior.
- [ ] Auth scheme and scopes confirmed.
- [ ] Pagination/rate-limit/partial-result behavior acceptable per tool.
