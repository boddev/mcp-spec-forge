# MCP Server Design — PokeAPI

> MCP server designed from an eval set of 12 questions over 98 documented API endpoint(s).

Generated by **mcp-spec-forge** (deterministic mode) on 2026-06-22T23:06:15.514Z.
Built from **12** eval questions over **98** documented endpoints.

## Server

- **Name:** PokeAPI
- **Transport:** stdio | streamable-http
- **Auth:** api_key (env: `API_KEY`)

## Coverage summary

- **Questions covered:** 12/12 (100%)

## Tools

The forge produced **10** task-oriented tools. Each wraps one or more API
endpoints so a question is answered in a single tool call rather than many round-trips.

### `summarize_ability`

Task-oriented tool for ability. Wraps 2 API endpoint(s), collapsing a list-then-detail (N+1) pattern into a single call so a question is answered without multiple round-trips. Example question it answers: "What are Pikachu's abilities and what does each one do?".

- **Entities:** ability
- **Covers questions:** q1, q6

**Underlying API calls (in order):**

1. `GET /api/v2/ability/` — Abilities provide passive effects for Pokémon in battle or in the overworld. Pokémon have multiple possible abilities but can have only one ability at a time. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Ability) for greater detail.
2. `GET /api/v2/ability/{id}/` — Abilities provide passive effects for Pokémon in battle or in the overworld. Pokémon have multiple possible abilities but can have only one ability at a time. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Ability) for greater detail. _(foreach `ability_list.items`, if `include_details == true`, concurrency 5, max 50)_

**Input schema:** `object{ id, q, max_items, include_details }`

**Output schema:** `object{ items, count, truncated }`

### `summarize_type`

Task-oriented tool for type. Wraps 2 API endpoint(s), collapsing a list-then-detail (N+1) pattern into a single call so a question is answered without multiple round-trips. Example question it answers: "List the first 20 Pokemon and show each one's types.".

- **Entities:** type
- **Covers questions:** q2, q10

**Underlying API calls (in order):**

1. `GET /api/v2/type/` — List types
2. `GET /api/v2/type/{id}/` — Get types _(foreach `type_list.items`, if `include_details == true`, concurrency 5, max 50)_

**Input schema:** `object{ id, q, max_items, include_details }`

**Output schema:** `object{ items, count, truncated }`

### `summarize_move_learn`

Task-oriented tool for move, learn, method. Wraps 2 API endpoint(s), collapsing a list-then-detail (N+1) pattern into a single call so a question is answered without multiple round-trips. Example question it answers: "What moves can Charizard learn, and what is the power and type of each move?".

- **Entities:** move, learn, method
- **Covers questions:** q3

**Underlying API calls (in order):**

1. `GET /api/v2/move-learn-method/{id}/` — Get move learn method
2. `GET /api/v2/move/{id}/` — Get move

**Input schema:** `object{ id, include_details }`

**Output schema:** `object{ id, name, names, language, url, descriptions, description }`

### `get_evolution_chain`

Task-oriented tool for evolution, chain. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "What is the evolution chain for Bulbasaur?".

- **Entities:** evolution, chain
- **Covers questions:** q4

**Underlying API calls (in order):**

1. `GET /api/v2/evolution-chain/{id}/` — Get evolution chain

**Input schema:** `object{ id }`

**Output schema:** `object{ id, baby_trigger_item, name, url, chain, evolution_details, evolves_to, is_baby, species }`

### `summarize_berry_flavor`

Task-oriented tool for berry, flavor. Wraps 2 API endpoint(s), collapsing a list-then-detail (N+1) pattern into a single call so a question is answered without multiple round-trips. Example question it answers: "Which berries affect a Pokemon's happiness, and what flavor is each one?".

- **Entities:** berry, flavor
- **Covers questions:** q5

**Underlying API calls (in order):**

1. `GET /api/v2/berry-flavor/` — List berry flavors
2. `GET /api/v2/berry-flavor/{id}/` — Get berries by flavor _(foreach `berry_flavor_list.items`, if `include_details == true`, concurrency 5, max 50)_

**Input schema:** `object{ id, q, max_items, include_details }`

**Output schema:** `object{ items, count, truncated }`

### `list_region`

Task-oriented tool for region. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "Describe the Kanto region and list the locations it contains.".

- **Entities:** region
- **Covers questions:** q7

**Underlying API calls (in order):**

1. `GET /api/v2/region/` — List regions

**Input schema:** `object{ q, max_items }`

**Output schema:** `object{ items, count, truncated }`

### `summarize_item_category`

Task-oriented tool for item, category. Wraps 2 API endpoint(s), collapsing a list-then-detail (N+1) pattern into a single call so a question is answered without multiple round-trips. Example question it answers: "What items are in the medicine category and what does each item do?".

- **Entities:** item, category
- **Covers questions:** q8

**Underlying API calls (in order):**

1. `GET /api/v2/item-category/{id}/` — Get item category
2. `GET /api/v2/item/{id}/` — Get item

**Input schema:** `object{ id, include_details }`

**Output schema:** `object{ id, name, items, url, names, language, pocket }`

### `list_stat`

Task-oriented tool for stat. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "What are the base stats of Mewtwo?".

- **Entities:** stat
- **Covers questions:** q9

**Underlying API calls (in order):**

1. `GET /api/v2/stat/` — List stats

**Input schema:** `object{ q, max_items }`

**Output schema:** `object{ items, count, truncated }`

### `list_pokemon_encounter`

Task-oriented tool for pokemon, encounter. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "What Pokemon can be found in the Viridian Forest and by what encounter method?".

- **Entities:** pokemon, encounter
- **Covers questions:** q11

**Underlying API calls (in order):**

1. `GET /api/v2/pokemon/{pokemon_id}/encounters` — Get pokemon encounter

**Input schema:** `object{ pokemon_id, max_items }`

**Output schema:** `object{ items, count, truncated }`

### `list_pokemon_species`

Task-oriented tool for pokemon, species. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "For the Gen 1 generation, which Pokemon species were introduced?".

- **Entities:** pokemon, species
- **Covers questions:** q12

**Underlying API calls (in order):**

1. `GET /api/v2/pokemon-species/` — List pokemon species

**Input schema:** `object{ q, max_items }`

**Output schema:** `object{ items, count, truncated }`

## Resources

- **api_capability_index** — Normalized index of the 98 documented API endpoints (method, path, params, response fields) the tools are built from.
- **eval_coverage_matrix** — Maps each evaluation question to the tool that answers it, with confidence and gaps.

## Prompts

- **answer_with_api_context** — System/template prompt instructing the model to select the single best task-oriented tool, pass minimal inputs, and avoid issuing redundant multi-call sequences.
- **route_question_to_tool** — Few-shot template mapping representative eval questions to the tool that should handle them.

## Eval coverage matrix

| Question | Intent / prompt | Covered by | Confidence | Notes |
|---|---|---|---|---|
| q1 | What are Pikachu's abilities and what does each one do? | summarize_ability | 0.42 | list-then-detail collapsed |
| q2 | List the first 20 Pokemon and show each one's types. | summarize_type | 0.36 | list-then-detail collapsed |
| q3 | What moves can Charizard learn, and what is the power and type of each move? | summarize_move_learn | 0.44 | list-then-detail collapsed |
| q4 | What is the evolution chain for Bulbasaur? | get_evolution_chain | 0.78 |  |
| q5 | Which berries affect a Pokemon's happiness, and what flavor is each one? | summarize_berry_flavor | 0.45 | list-then-detail collapsed |
| q6 | What are the effects of the ability Overgrow? | summarize_ability | 0.58 |  |
| q7 | Describe the Kanto region and list the locations it contains. | list_region | 0.46 |  |
| q8 | What items are in the medicine category and what does each item do? | summarize_item_category | 0.44 | list-then-detail collapsed |
| q9 | What are the base stats of Mewtwo? | list_stat | 0.43 |  |
| q10 | List all Pokemon types and what each type is strong against. | summarize_type | 0.42 | list-then-detail collapsed |
| q11 | What Pokemon can be found in the Viridian Forest and by what encounter method? | list_pokemon_encounter | 0.39 |  |
| q12 | For the Gen 1 generation, which Pokemon species were introduced? | list_pokemon_species | 0.53 |  |

## Review checklist

- [ ] Every uncovered question has a planned tool or is out of scope.
- [ ] Broad/god tools are split or justified.
- [ ] HTML-derived endpoints verified against real API behavior.
- [ ] Auth scheme and scopes confirmed.
- [ ] Pagination/rate-limit/partial-result behavior acceptable per tool.
