# MCP server design — generated by mcp-spec-forge
# Canonical, machine-readable spec. See mcp-design.md for rationale.
server:
  name: FHIR R4
  description: MCP server designed from an eval set of 12 questions over 4601 documented API endpoint(s).
  version: 0.1.0
  transport: stdio | streamable-http
  baseUrl: https://hapi.fhir.org/baseR4
auth:
  type: none
tools:
  - name: summarize_patient
    description: >-
      Task-oriented tool for patient. Wraps 2 API endpoint(s), collapsing a list-then-detail (N+1)
      pattern into a single call so a question is answered without multiple round-trips. Example
      question it answers: "Find all patients named Smith and show each patient's birth date and
      gender.".
    evalCoverage:
      - q1
      - q6
      - q7
      - q12
    entities:
      - patient
    inputSchema:
      type: object
      required:
        - id
      properties:
        id:
          type: string
          description: The resource ID
        max_items:
          type: integer
          description: Maximum records to return (caps pagination).
          default: 50
        include_details:
          type: boolean
          description: Expand each result with its detail/sub-resource calls in one round-trip.
          default: true
    outputSchema:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              summary:
                type: string
        count:
          type: integer
        truncated:
          type: boolean
          description: True if max_items capped the result.
    underlyingCalls:
      - id: call_1
        endpoint: GET /Patient
        purpose: 'search-type: Search for Patient instances'
      - id: call_2
        endpoint: GET /Patient/{id}
        purpose: 'read-instance: Read Patient instance'
        params:
          id: '{id}'
        foreach: call_1.items
        condition: include_details == true
        concurrency: 5
        maxItems: 50
    pagination:
      strategy: auto_paginate_until_limit
      maxPages: 5
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: true
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: find_observation
    description: >-
      Task-oriented tool for observation. Wraps 1 API endpoint(s) so a question is answered without
      multiple round-trips. Example question it answers: "What are the most recent vital-sign
      observations for patient 12345?".
    evalCoverage:
      - q2
    entities:
      - observation
    inputSchema:
      type: object
      properties:
        status:
          type: string
          description: The status of the observation
        max_items:
          type: integer
          description: Maximum records to return (caps pagination).
          default: 50
    outputSchema:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              summary:
                type: string
        count:
          type: integer
        truncated:
          type: boolean
          description: True if max_items capped the result.
    underlyingCalls:
      - id: call_1
        endpoint: GET /Observation
        purpose: 'search-type: Search for Observation instances'
    pagination:
      strategy: auto_paginate_until_limit
      maxPages: 5
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: find_condition
    description: >-
      Task-oriented tool for condition. Wraps 1 API endpoint(s) so a question is answered without
      multiple round-trips. Example question it answers: "List the active conditions (problems)
      recorded for a given patient.".
    evalCoverage:
      - q3
    entities:
      - condition
    inputSchema:
      type: object
      properties:
        max_items:
          type: integer
          description: Maximum records to return (caps pagination).
          default: 50
    outputSchema:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              summary:
                type: string
        count:
          type: integer
        truncated:
          type: boolean
          description: True if max_items capped the result.
    underlyingCalls:
      - id: call_1
        endpoint: GET /Condition
        purpose: 'search-type: Search for Condition instances'
    pagination:
      strategy: auto_paginate_until_limit
      maxPages: 5
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: get_medication
    description: >-
      Task-oriented tool for medication. Wraps 1 API endpoint(s) so a question is answered without
      multiple round-trips. Example question it answers: "What medications has patient 67890 been
      prescribed and what is the dosage of each?".
    evalCoverage:
      - q4
    entities:
      - medication
    inputSchema:
      type: object
      required:
        - id
      properties:
        id:
          type: string
          description: The resource ID
    outputSchema:
      type: object
      properties:
        id:
          type: string
        summary:
          type: string
    underlyingCalls:
      - id: call_1
        endpoint: GET /Medication/{id}
        purpose: 'read-instance: Read Medication instance'
        params:
          id: '{id}'
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: summarize_encounter
    description: >-
      Task-oriented tool for encounter. Wraps 2 API endpoint(s), collapsing a list-then-detail (N+1)
      pattern into a single call so a question is answered without multiple round-trips. Example
      question it answers: "Show all encounters for a patient and the reason and date of each
      visit.".
    evalCoverage:
      - q5
    entities:
      - encounter
    inputSchema:
      type: object
      required:
        - id
      properties:
        id:
          type: string
          description: The resource ID
        type:
          type: string
          description: "Multiple Resources: \r\n\r\n* [AllergyIntolerance](https://hl7.org/fhir/R4/allergyintolerance.html): allergy | intolerance - Underlying mechanism (if known)\r\n* [Composition](https://hl7.org/fhir/R4/composition.html): Kind of composition (LOINC if possible)\r\n* [DocumentManifest](https://hl7.org/fhir/R4/documentmanifest.html): Kind of document set\r\n* [DocumentReference](https://hl7.org/fhir/R4/documentreference.html): Kind of document (LOINC if possible)\r\n* [Encounter](https://hl7.org/fhir/R4/encounter.html): Specific type of encounter\r\n* [EpisodeOfCare](https://hl7.org/fhir/R4/episodeofcare.html): Type/class  - e.g. specialist referral, disease management"
        status:
          type: string
          description: planned | arrived | triaged | in-progress | onleave | finished | cancelled +
        max_items:
          type: integer
          description: Maximum records to return (caps pagination).
          default: 50
        include_details:
          type: boolean
          description: Expand each result with its detail/sub-resource calls in one round-trip.
          default: true
    outputSchema:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              summary:
                type: string
        count:
          type: integer
        truncated:
          type: boolean
          description: True if max_items capped the result.
    underlyingCalls:
      - id: call_1
        endpoint: GET /Encounter
        purpose: 'search-type: Search for Encounter instances'
      - id: call_2
        endpoint: GET /Encounter/{id}
        purpose: 'read-instance: Read Encounter instance'
        params:
          id: '{id}'
        foreach: call_1.items
        condition: include_details == true
        concurrency: 5
        maxItems: 50
    pagination:
      strategy: auto_paginate_until_limit
      maxPages: 5
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: true
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: get_allergyintolerance
    description: >-
      Task-oriented tool for allergyintolerance. Wraps 1 API endpoint(s) so a question is answered
      without multiple round-trips. Example question it answers: "What allergies and intolerances
      are documented for patient 12345?".
    evalCoverage:
      - q8
    entities:
      - allergyintolerance
    inputSchema:
      type: object
      required:
        - id
      properties:
        id:
          type: string
          description: The resource ID
    outputSchema:
      type: object
      properties:
        id:
          type: string
        summary:
          type: string
    underlyingCalls:
      - id: call_1
        endpoint: GET /AllergyIntolerance/{id}
        purpose: 'read-instance: Read AllergyIntolerance instance'
        params:
          id: '{id}'
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: find_immunization
    description: >-
      Task-oriented tool for immunization. Wraps 1 API endpoint(s) so a question is answered without
      multiple round-trips. Example question it answers: "List all immunizations a patient has
      received and the date of each.".
    evalCoverage:
      - q9
    entities:
      - immunization
    inputSchema:
      type: object
      properties:
        status:
          type: string
          description: Immunization event status
        max_items:
          type: integer
          description: Maximum records to return (caps pagination).
          default: 50
    outputSchema:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              summary:
                type: string
        count:
          type: integer
        truncated:
          type: boolean
          description: True if max_items capped the result.
    underlyingCalls:
      - id: call_1
        endpoint: GET /Immunization
        purpose: 'search-type: Search for Immunization instances'
    pagination:
      strategy: auto_paginate_until_limit
      maxPages: 5
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: find_careteam
    description: >-
      Task-oriented tool for careteam. Wraps 1 API endpoint(s) so a question is answered without
      multiple round-trips. Example question it answers: "Which practitioners are associated with a
      patient's care team?".
    evalCoverage:
      - q10
    entities:
      - careteam
    inputSchema:
      type: object
      properties:
        status:
          type: string
          description: proposed | active | suspended | inactive | entered-in-error
        max_items:
          type: integer
          description: Maximum records to return (caps pagination).
          default: 50
    outputSchema:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              summary:
                type: string
        count:
          type: integer
        truncated:
          type: boolean
          description: True if max_items capped the result.
    underlyingCalls:
      - id: call_1
        endpoint: GET /CareTeam
        purpose: 'search-type: Search for CareTeam instances'
    pagination:
      strategy: auto_paginate_until_limit
      maxPages: 5
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: find_diagnosticreport
    description: >-
      Task-oriented tool for diagnosticreport. Wraps 1 API endpoint(s) so a question is answered
      without multiple round-trips. Example question it answers: "Find diagnostic reports for a
      patient and summarize the conclusion of each.".
    evalCoverage:
      - q11
    entities:
      - diagnosticreport
    inputSchema:
      type: object
      properties:
        status:
          type: string
          description: The status of the report
        max_items:
          type: integer
          description: Maximum records to return (caps pagination).
          default: 50
    outputSchema:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              summary:
                type: string
        count:
          type: integer
        truncated:
          type: boolean
          description: True if max_items capped the result.
    underlyingCalls:
      - id: call_1
        endpoint: GET /DiagnosticReport
        purpose: 'search-type: Search for DiagnosticReport instances'
    pagination:
      strategy: auto_paginate_until_limit
      maxPages: 5
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
resources:
  - name: api_capability_index
    description: >-
      Normalized index of the 4601 documented API endpoints (method, path, params, response fields)
      the tools are built from.
    uri: forge://capabilities
  - name: eval_coverage_matrix
    description: Maps each evaluation question to the tool that answers it, with confidence and gaps.
    uri: forge://coverage
prompts:
  - name: answer_with_api_context
    description: >-
      System/template prompt instructing the model to select the single best task-oriented tool,
      pass minimal inputs, and avoid issuing redundant multi-call sequences.
  - name: route_question_to_tool
    description: Few-shot template mapping representative eval questions to the tool that should handle them.
meta:
  generatedAt: '2026-06-22T22:49:45.167Z'
  generator: mcp-spec-forge
  endpointCount: 4601
  questionCount: 12
  mode: deterministic
