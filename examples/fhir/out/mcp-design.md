# MCP Server Design — FHIR R4

> MCP server designed from an eval set of 12 questions over 4601 documented API endpoint(s).

Generated by **mcp-spec-forge** (deterministic mode) on 2026-06-22T22:49:45.167Z.
Built from **12** eval questions over **4601** documented endpoints.

## Server

- **Name:** FHIR R4
- **Transport:** stdio | streamable-http
- **Auth:** none

## Coverage summary

- **Questions covered:** 12/12 (100%)

## Tools

The forge produced **9** task-oriented tools. Each wraps one or more API
endpoints so a question is answered in a single tool call rather than many round-trips.

### `summarize_patient`

Task-oriented tool for patient. Wraps 2 API endpoint(s), collapsing a list-then-detail (N+1) pattern into a single call so a question is answered without multiple round-trips. Example question it answers: "Find all patients named Smith and show each patient's birth date and gender.".

- **Entities:** patient
- **Covers questions:** q1, q6, q7, q12

**Underlying API calls (in order):**

1. `GET /Patient` — search-type: Search for Patient instances
2. `GET /Patient/{id}` — read-instance: Read Patient instance _(foreach `call_1.items`, if `include_details == true`, concurrency 5, max 50)_

**Input schema:** `object{ id, max_items, include_details }`

**Output schema:** `object{ items, count, truncated }`

### `find_observation`

Task-oriented tool for observation. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "What are the most recent vital-sign observations for patient 12345?".

- **Entities:** observation
- **Covers questions:** q2

**Underlying API calls (in order):**

1. `GET /Observation` — search-type: Search for Observation instances

**Input schema:** `object{ status, max_items }`

**Output schema:** `object{ items, count, truncated }`

### `find_condition`

Task-oriented tool for condition. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "List the active conditions (problems) recorded for a given patient.".

- **Entities:** condition
- **Covers questions:** q3

**Underlying API calls (in order):**

1. `GET /Condition` — search-type: Search for Condition instances

**Input schema:** `object{ max_items }`

**Output schema:** `object{ items, count, truncated }`

### `get_medication`

Task-oriented tool for medication. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "What medications has patient 67890 been prescribed and what is the dosage of each?".

- **Entities:** medication
- **Covers questions:** q4

**Underlying API calls (in order):**

1. `GET /Medication/{id}` — read-instance: Read Medication instance

**Input schema:** `object{ id }`

**Output schema:** `object{ id, summary }`

### `summarize_encounter`

Task-oriented tool for encounter. Wraps 2 API endpoint(s), collapsing a list-then-detail (N+1) pattern into a single call so a question is answered without multiple round-trips. Example question it answers: "Show all encounters for a patient and the reason and date of each visit.".

- **Entities:** encounter
- **Covers questions:** q5

**Underlying API calls (in order):**

1. `GET /Encounter` — search-type: Search for Encounter instances
2. `GET /Encounter/{id}` — read-instance: Read Encounter instance _(foreach `call_1.items`, if `include_details == true`, concurrency 5, max 50)_

**Input schema:** `object{ id, type, status, max_items, include_details }`

**Output schema:** `object{ items, count, truncated }`

### `get_allergyintolerance`

Task-oriented tool for allergyintolerance. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "What allergies and intolerances are documented for patient 12345?".

- **Entities:** allergyintolerance
- **Covers questions:** q8

**Underlying API calls (in order):**

1. `GET /AllergyIntolerance/{id}` — read-instance: Read AllergyIntolerance instance

**Input schema:** `object{ id }`

**Output schema:** `object{ id, summary }`

### `find_immunization`

Task-oriented tool for immunization. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "List all immunizations a patient has received and the date of each.".

- **Entities:** immunization
- **Covers questions:** q9

**Underlying API calls (in order):**

1. `GET /Immunization` — search-type: Search for Immunization instances

**Input schema:** `object{ status, max_items }`

**Output schema:** `object{ items, count, truncated }`

### `find_careteam`

Task-oriented tool for careteam. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "Which practitioners are associated with a patient's care team?".

- **Entities:** careteam
- **Covers questions:** q10

**Underlying API calls (in order):**

1. `GET /CareTeam` — search-type: Search for CareTeam instances

**Input schema:** `object{ status, max_items }`

**Output schema:** `object{ items, count, truncated }`

### `find_diagnosticreport`

Task-oriented tool for diagnosticreport. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "Find diagnostic reports for a patient and summarize the conclusion of each.".

- **Entities:** diagnosticreport
- **Covers questions:** q11

**Underlying API calls (in order):**

1. `GET /DiagnosticReport` — search-type: Search for DiagnosticReport instances

**Input schema:** `object{ status, max_items }`

**Output schema:** `object{ items, count, truncated }`

## Resources

- **api_capability_index** — Normalized index of the 4601 documented API endpoints (method, path, params, response fields) the tools are built from.
- **eval_coverage_matrix** — Maps each evaluation question to the tool that answers it, with confidence and gaps.

## Prompts

- **answer_with_api_context** — System/template prompt instructing the model to select the single best task-oriented tool, pass minimal inputs, and avoid issuing redundant multi-call sequences.
- **route_question_to_tool** — Few-shot template mapping representative eval questions to the tool that should handle them.

## Eval coverage matrix

| Question | Intent / prompt | Covered by | Confidence | Notes |
|---|---|---|---|---|
| q1 | Find all patients named Smith and show each patient's birth date and gender. | summarize_patient | 0.21 | list-then-detail collapsed |
| q2 | What are the most recent vital-sign observations for patient 12345? | find_observation | 0.22 |  |
| q3 | List the active conditions (problems) recorded for a given patient. | find_condition | 0.23 |  |
| q4 | What medications has patient 67890 been prescribed and what is the dosage of each? | get_medication | 0.23 |  |
| q5 | Show all encounters for a patient and the reason and date of each visit. | summarize_encounter | 0.23 | list-then-detail collapsed |
| q6 | Retrieve the details of a specific patient by id including their address and contact info. | summarize_patient | 0.28 |  |
| q7 | Find lab results with abnormal values for a patient over the last year. | summarize_patient | 0.23 |  |
| q8 | What allergies and intolerances are documented for patient 12345? | get_allergyintolerance | 0.20 |  |
| q9 | List all immunizations a patient has received and the date of each. | find_immunization | 0.26 |  |
| q10 | Which practitioners are associated with a patient's care team? | find_careteam | 0.30 |  |
| q11 | Find diagnostic reports for a patient and summarize the conclusion of each. | find_diagnosticreport | 0.27 |  |
| q12 | Show the procedures performed on patient 67890 and when each occurred. | summarize_patient | 0.23 |  |

## Review checklist

- [ ] Every uncovered question has a planned tool or is out of scope.
- [ ] Broad/god tools are split or justified.
- [ ] HTML-derived endpoints verified against real API behavior.
- [ ] Auth scheme and scopes confirmed.
- [ ] Pagination/rate-limit/partial-result behavior acceptable per tool.
