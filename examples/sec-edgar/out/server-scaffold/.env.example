# Base URL of the target API (overrides the spec default).
API_BASE_URL=https://api.edgarfiling.sec.gov

# Credential the API requires (auth type: bearer_token).
API_TOKEN=

# Port for the remote (Streamable HTTP) server.
PORT=3000
