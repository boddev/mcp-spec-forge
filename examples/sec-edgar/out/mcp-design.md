# MCP Server Design — SEC EDGAR Filing

> MCP server designed from an eval set of 14 questions over 18 documented API endpoint(s).

Generated by **mcp-spec-forge** (deterministic mode) on 2026-06-22T23:05:33.853Z.
Built from **14** eval questions over **18** documented endpoints.

## Server

- **Name:** SEC EDGAR Filing
- **Transport:** stdio | streamable-http
- **Auth:** bearer_token (env: `API_TOKEN`)

## Coverage summary

- **Questions covered:** 14/14 (100%)

## Tools

The forge produced **14** task-oriented tools. Each wraps one or more API
endpoints so a question is answered in a single tool call rather than many round-trips.

### `get_view_filer`

Task-oriented tool for view, filer, account. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "How do I view the filer account information for a given CIK?".

- **Entities:** view, filer, account
- **Covers questions:** q1

**Underlying API calls (in order):**

1. `GET /fm/{cik}` — View filer account information

**Input schema:** `object{ cik }`

**Output schema:** `object{ tracking, locator, messages, type, content, filerInfo, cik, stateCode, irsNumber, entityStatus, entityType, dateIncorporated }`

### `list_status`

Task-oriented tool for status. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "Is the EDGAR system currently online and accepting filings?".

- **Entities:** status
- **Covers questions:** q2

**Underlying API calls (in order):**

1. `GET /status` — EDGAR System Status

**Input schema:** `object{ max_items }`

**Output schema:** `object{ items, count, truncated }`

### `list_verify`

Task-oriented tool for verify. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "Verify that my filing credentials are valid before I submit a filing.".

- **Entities:** verify
- **Covers questions:** q3

**Underlying API calls (in order):**

1. `GET /fm/{cik}/verify` — Filing credentials verification

**Input schema:** `object{ cik, max_items }`

**Output schema:** `object{ items, count, truncated }`

### `create_submission_single`

Task-oriented tool for submission, single, live. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "Submit a single live filing to EDGAR.".

- **Entities:** submission, single, live
- **Covers questions:** q4

**Underlying API calls (in order):**

1. `POST /submission/single/live` — Single Live Submission

**Input schema:** `object{  }`

**Output schema:** `object{ tracking, locator, accessionNumber, messages, type, content, mode, status, transmissionStatus }`

### `create_submission_bulk`

Task-oriented tool for submission, bulk, live. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "Submit multiple filings at once in one bulk live submission.".

- **Entities:** submission, bulk, live
- **Covers questions:** q5

**Underlying API calls (in order):**

1. `POST /submission/bulk/live` — Bulk Live Submission

**Input schema:** `object{  }`

**Output schema:** `object{ tracking, locator, status, responses, accessionNumber, messages, type, content, mode, final, transmissionStatus }`

### `list_submission_status`

Task-oriented tool for submission, status. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "Check the processing status of one submission by its accession number.".

- **Entities:** submission, status
- **Covers questions:** q6

**Underlying API calls (in order):**

1. `GET /submission/{accessionNumber}/status` — Check Single Submission Status

**Input schema:** `object{ accessionNumber, max_items }`

**Output schema:** `object{ items, count, truncated }`

### `create_submission_status`

Task-oriented tool for submission, status. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "Check the status of several of my submissions at the same time.".

- **Entities:** submission, status
- **Covers questions:** q7

**Underlying API calls (in order):**

1. `POST /submission/status` — Check Multiple Submission Statuses

**Input schema:** `object{  }`

**Output schema:** `object{ tracking, locator, responses, submissionAccessionNumber, submissionFormType, items, accessionNumber, messages, type, content, mode, status }`

### `list_individual`

Task-oriented tool for individual. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "List the individuals authorized on the account and the role each one has.".

- **Entities:** individual
- **Covers questions:** q8

**Underlying API calls (in order):**

1. `GET /fm/{cik}/individuals` — View individuals

**Input schema:** `object{ cik, max_items }`

**Output schema:** `object{ items, count, truncated }`

### `create_individual`

Task-oriented tool for individual. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "Add a new individual to the account.".

- **Entities:** individual
- **Covers questions:** q9

**Underlying API calls (in order):**

1. `POST /fm/{cik}/individuals` — Add individuals

**Input schema:** `object{ cik }`

**Output schema:** `object{ tracking, locator, messages, type, content, filerInfo, cik, stateCode, irsNumber, entityStatus, entityType, dateIncorporated }`

### `update_individual`

Task-oriented tool for individual. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "Change the role assigned to an individual on my account.".

- **Entities:** individual
- **Covers questions:** q10

**Underlying API calls (in order):**

1. `PUT /fm/{cik}/individuals` — Change roles

**Input schema:** `object{ cik }`

**Output schema:** `object{ tracking, locator, messages, type, content, filerInfo, cik, stateCode, irsNumber, entityStatus, entityType, dateIncorporated }`

### `delete_individual`

Task-oriented tool for individual. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "Remove an individual from the account.".

- **Entities:** individual
- **Covers questions:** q11

**Underlying API calls (in order):**

1. `DELETE /fm/{cik}/individuals` — Remove individuals

**Input schema:** `object{ cik }`

**Output schema:** `object{ tracking, locator, messages, type, content, filerInfo, cik, stateCode, irsNumber, entityStatus, entityType, dateIncorporated }`

### `create_ccc`

Task-oriented tool for ccc. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "Generate a new CCC (CIK Confirmation Code) for the CIK.".

- **Entities:** ccc
- **Covers questions:** q12

**Underlying API calls (in order):**

1. `POST /fm/{cik}/ccc` — Generate CCC

**Input schema:** `object{ cik }`

**Output schema:** `object{ tracking, locator, messages, type, content, filerCCC }`

### `list_delegation`

Task-oriented tool for delegation. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "View the delegations associated with the CIK.".

- **Entities:** delegation
- **Covers questions:** q13

**Underlying API calls (in order):**

1. `GET /fm/{cik}/delegations` — View delegations

**Input schema:** `object{ cik, max_items }`

**Output schema:** `object{ items, count, truncated }`

### `create_delegation`

Task-oriented tool for delegation. Wraps 1 API endpoint(s) so a question is answered without multiple round-trips. Example question it answers: "Send a delegation invitation to another filer.".

- **Entities:** delegation
- **Covers questions:** q14

**Underlying API calls (in order):**

1. `POST /fm/{cik}/delegations` — Send delegation invitations

**Input schema:** `object{ cik }`

**Output schema:** `object{ tracking, locator, messages, type, content, filerInfo, cik, stateCode, irsNumber, entityStatus, entityType, dateIncorporated }`

## Resources

- **api_capability_index** — Normalized index of the 18 documented API endpoints (method, path, params, response fields) the tools are built from.
- **eval_coverage_matrix** — Maps each evaluation question to the tool that answers it, with confidence and gaps.

## Prompts

- **answer_with_api_context** — System/template prompt instructing the model to select the single best task-oriented tool, pass minimal inputs, and avoid issuing redundant multi-call sequences.
- **route_question_to_tool** — Few-shot template mapping representative eval questions to the tool that should handle them.

## Eval coverage matrix

| Question | Intent / prompt | Covered by | Confidence | Notes |
|---|---|---|---|---|
| q1 | How do I view the filer account information for a given CIK? | get_view_filer | 0.60 |  |
| q2 | Is the EDGAR system currently online and accepting filings? | list_status | 0.15 |  |
| q3 | Verify that my filing credentials are valid before I submit a filing. | list_verify | 0.40 |  |
| q4 | Submit a single live filing to EDGAR. | create_submission_single | 0.55 |  |
| q5 | Submit multiple filings at once in one bulk live submission. | create_submission_bulk | 0.60 |  |
| q6 | Check the processing status of one submission by its accession number. | list_submission_status | 0.75 |  |
| q7 | Check the status of several of my submissions at the same time. | create_submission_status | 0.60 |  |
| q8 | List the individuals authorized on the account and the role each one has. | list_individual | 0.38 |  |
| q9 | Add a new individual to the account. | create_individual | 0.55 |  |
| q10 | Change the role assigned to an individual on my account. | update_individual | 0.57 |  |
| q11 | Remove an individual from the account. | delete_individual | 0.65 |  |
| q12 | Generate a new CCC (CIK Confirmation Code) for the CIK. | create_ccc | 0.42 |  |
| q13 | View the delegations associated with the CIK. | list_delegation | 0.58 |  |
| q14 | Send a delegation invitation to another filer. | create_delegation | 0.56 |  |

## Review checklist

- [ ] Every uncovered question has a planned tool or is out of scope.
- [ ] Broad/god tools are split or justified.
- [ ] HTML-derived endpoints verified against real API behavior.
- [ ] Auth scheme and scopes confirmed.
- [ ] Pagination/rate-limit/partial-result behavior acceptable per tool.
