# MCP server design — generated by mcp-spec-forge
# Canonical, machine-readable spec. See mcp-design.md for rationale.
server:
  name: SEC EDGAR Filing
  description: MCP server designed from an eval set of 14 questions over 18 documented API endpoint(s).
  version: 0.1.0
  transport: stdio | streamable-http
  baseUrl: https://api.edgarfiling.sec.gov
auth:
  type: bearer_token
  envVar: API_TOKEN
tools:
  - name: get_view_filer
    description: >-
      Task-oriented tool for view, filer, account. Wraps 1 API endpoint(s) so a question is answered
      without multiple round-trips. Example question it answers: "How do I view the filer account
      information for a given CIK?".
    evalCoverage:
      - q1
    entities:
      - view
      - filer
      - account
    inputSchema:
      type: object
      required:
        - cik
      properties:
        cik:
          type: string
    outputSchema:
      type: object
      properties:
        tracking:
          type: string
        locator:
          type: string
        messages:
          type: string
        type:
          type: string
        content:
          type: string
        filerInfo:
          type: string
        cik:
          type: string
        stateCode:
          type: string
        irsNumber:
          type: string
        entityStatus:
          type: string
        entityType:
          type: string
        dateIncorporated:
          type: string
    underlyingCalls:
      - id: View Filer Account Information
        endpoint: GET /fm/{cik}
        purpose: View filer account information
        params:
          cik: '{cik}'
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: list_status
    description: >-
      Task-oriented tool for status. Wraps 1 API endpoint(s) so a question is answered without
      multiple round-trips. Example question it answers: "Is the EDGAR system currently online and
      accepting filings?".
    evalCoverage:
      - q2
    entities:
      - status
    inputSchema:
      type: object
      properties:
        max_items:
          type: integer
          description: Maximum records to return (caps pagination).
          default: 50
    outputSchema:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              tracking:
                type: string
              locator:
                type: string
              condition:
                type: string
              message:
                type: string
        count:
          type: integer
        truncated:
          type: boolean
          description: True if max_items capped the result.
    underlyingCalls:
      - id: EDGAR System Status
        endpoint: GET /status
        purpose: EDGAR System Status
    pagination:
      strategy: auto_paginate_until_limit
      maxPages: 5
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: list_verify
    description: >-
      Task-oriented tool for verify. Wraps 1 API endpoint(s) so a question is answered without
      multiple round-trips. Example question it answers: "Verify that my filing credentials are
      valid before I submit a filing.".
    evalCoverage:
      - q3
    entities:
      - verify
    inputSchema:
      type: object
      required:
        - cik
      properties:
        cik:
          type: string
        max_items:
          type: integer
          description: Maximum records to return (caps pagination).
          default: 50
    outputSchema:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              tracking:
                type: string
              locator:
                type: string
              messages:
                type: string
              type:
                type: string
              content:
                type: string
              canFile:
                type: string
              confirmationDueDate:
                type: string
              filerApiTokenExpirationDate:
                type: string
              userApiTokenExpirationDate:
                type: string
        count:
          type: integer
        truncated:
          type: boolean
          description: True if max_items capped the result.
    underlyingCalls:
      - id: Filing Credentials Verification
        endpoint: GET /fm/{cik}/verify
        purpose: Filing credentials verification
        params:
          cik: '{cik}'
    pagination:
      strategy: auto_paginate_until_limit
      maxPages: 5
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: create_submission_single
    description: >-
      Task-oriented tool for submission, single, live. Wraps 1 API endpoint(s) so a question is
      answered without multiple round-trips. Example question it answers: "Submit a single live
      filing to EDGAR.".
    evalCoverage:
      - q4
    entities:
      - submission
      - single
      - live
    inputSchema:
      type: object
      properties: {}
    outputSchema:
      type: object
      properties:
        tracking:
          type: string
        locator:
          type: string
        accessionNumber:
          type: string
        messages:
          type: string
        type:
          type: string
        content:
          type: string
        mode:
          type: string
        status:
          type: string
        transmissionStatus:
          type: string
    underlyingCalls:
      - id: Single Live Submission
        endpoint: POST /submission/single/live
        purpose: Single Live Submission
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: create_submission_bulk
    description: >-
      Task-oriented tool for submission, bulk, live. Wraps 1 API endpoint(s) so a question is
      answered without multiple round-trips. Example question it answers: "Submit multiple filings
      at once in one bulk live submission.".
    evalCoverage:
      - q5
    entities:
      - submission
      - bulk
      - live
    inputSchema:
      type: object
      properties: {}
    outputSchema:
      type: object
      properties:
        tracking:
          type: string
        locator:
          type: string
        status:
          type: string
        responses:
          type: string
        accessionNumber:
          type: string
        messages:
          type: string
        type:
          type: string
        content:
          type: string
        mode:
          type: string
        final:
          type: string
        transmissionStatus:
          type: string
    underlyingCalls:
      - id: Bulk Live Submission
        endpoint: POST /submission/bulk/live
        purpose: Bulk Live Submission
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: list_submission_status
    description: >-
      Task-oriented tool for submission, status. Wraps 1 API endpoint(s) so a question is answered
      without multiple round-trips. Example question it answers: "Check the processing status of one
      submission by its accession number.".
    evalCoverage:
      - q6
    entities:
      - submission
      - status
    inputSchema:
      type: object
      required:
        - accessionNumber
      properties:
        accessionNumber:
          type: string
          description: The accession number returned when a submission is successfully transmitted.
        max_items:
          type: integer
          description: Maximum records to return (caps pagination).
          default: 50
    outputSchema:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              submissionAccessionNumber:
                type: string
              submissionFormType:
                type: string
              items:
                type: string
              tracking:
                type: string
              locator:
                type: string
              accessionNumber:
                type: string
              messages:
                type: string
              type:
                type: string
              content:
                type: string
              mode:
                type: string
              status:
                type: string
              company:
                type: string
        count:
          type: integer
        truncated:
          type: boolean
          description: True if max_items capped the result.
    underlyingCalls:
      - id: Check Single Submission Status
        endpoint: GET /submission/{accessionNumber}/status
        purpose: Check Single Submission Status
        params:
          accessionNumber: '{accessionNumber}'
    pagination:
      strategy: auto_paginate_until_limit
      maxPages: 5
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: create_submission_status
    description: >-
      Task-oriented tool for submission, status. Wraps 1 API endpoint(s) so a question is answered
      without multiple round-trips. Example question it answers: "Check the status of several of my
      submissions at the same time.".
    evalCoverage:
      - q7
    entities:
      - submission
      - status
    inputSchema:
      type: object
      properties: {}
    outputSchema:
      type: object
      properties:
        tracking:
          type: string
        locator:
          type: string
        responses:
          type: string
        submissionAccessionNumber:
          type: string
        submissionFormType:
          type: string
        items:
          type: string
        accessionNumber:
          type: string
        messages:
          type: string
        type:
          type: string
        content:
          type: string
        mode:
          type: string
        status:
          type: string
    underlyingCalls:
      - id: Check Multiple Submission Statuses
        endpoint: POST /submission/status
        purpose: Check Multiple Submission Statuses
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: list_individual
    description: >-
      Task-oriented tool for individual. Wraps 1 API endpoint(s) so a question is answered without
      multiple round-trips. Example question it answers: "List the individuals authorized on the
      account and the role each one has.".
    evalCoverage:
      - q8
    entities:
      - individual
    inputSchema:
      type: object
      required:
        - cik
      properties:
        cik:
          type: string
        max_items:
          type: integer
          description: Maximum records to return (caps pagination).
          default: 50
    outputSchema:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              tracking:
                type: string
              locator:
                type: string
              messages:
                type: string
              type:
                type: string
              content:
                type: string
              individuals:
                type: string
              email:
                type: string
              firstName:
                type: string
              middleName:
                type: string
              lastName:
                type: string
              businessPhoneNumber:
                type: string
              delegatedCik:
                type: string
        count:
          type: integer
        truncated:
          type: boolean
          description: True if max_items capped the result.
    underlyingCalls:
      - id: View Individuals
        endpoint: GET /fm/{cik}/individuals
        purpose: View individuals
        params:
          cik: '{cik}'
    pagination:
      strategy: auto_paginate_until_limit
      maxPages: 5
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: create_individual
    description: >-
      Task-oriented tool for individual. Wraps 1 API endpoint(s) so a question is answered without
      multiple round-trips. Example question it answers: "Add a new individual to the account.".
    evalCoverage:
      - q9
    entities:
      - individual
    inputSchema:
      type: object
      required:
        - cik
      properties:
        cik:
          type: string
    outputSchema:
      type: object
      properties:
        tracking:
          type: string
        locator:
          type: string
        messages:
          type: string
        type:
          type: string
        content:
          type: string
        filerInfo:
          type: string
        cik:
          type: string
        stateCode:
          type: string
        irsNumber:
          type: string
        entityStatus:
          type: string
        entityType:
          type: string
        dateIncorporated:
          type: string
    underlyingCalls:
      - id: Add Individuals
        endpoint: POST /fm/{cik}/individuals
        purpose: Add individuals
        params:
          cik: '{cik}'
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: update_individual
    description: >-
      Task-oriented tool for individual. Wraps 1 API endpoint(s) so a question is answered without
      multiple round-trips. Example question it answers: "Change the role assigned to an individual
      on my account.".
    evalCoverage:
      - q10
    entities:
      - individual
    inputSchema:
      type: object
      required:
        - cik
      properties:
        cik:
          type: string
    outputSchema:
      type: object
      properties:
        tracking:
          type: string
        locator:
          type: string
        messages:
          type: string
        type:
          type: string
        content:
          type: string
        filerInfo:
          type: string
        cik:
          type: string
        stateCode:
          type: string
        irsNumber:
          type: string
        entityStatus:
          type: string
        entityType:
          type: string
        dateIncorporated:
          type: string
    underlyingCalls:
      - id: Change Roles
        endpoint: PUT /fm/{cik}/individuals
        purpose: Change roles
        params:
          cik: '{cik}'
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: delete_individual
    description: >-
      Task-oriented tool for individual. Wraps 1 API endpoint(s) so a question is answered without
      multiple round-trips. Example question it answers: "Remove an individual from the account.".
    evalCoverage:
      - q11
    entities:
      - individual
    inputSchema:
      type: object
      required:
        - cik
      properties:
        cik:
          type: string
    outputSchema:
      type: object
      properties:
        tracking:
          type: string
        locator:
          type: string
        messages:
          type: string
        type:
          type: string
        content:
          type: string
        filerInfo:
          type: string
        cik:
          type: string
        stateCode:
          type: string
        irsNumber:
          type: string
        entityStatus:
          type: string
        entityType:
          type: string
        dateIncorporated:
          type: string
    underlyingCalls:
      - id: Remove Individuals
        endpoint: DELETE /fm/{cik}/individuals
        purpose: Remove individuals
        params:
          cik: '{cik}'
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: create_ccc
    description: >-
      Task-oriented tool for ccc. Wraps 1 API endpoint(s) so a question is answered without multiple
      round-trips. Example question it answers: "Generate a new CCC (CIK Confirmation Code) for the
      CIK.".
    evalCoverage:
      - q12
    entities:
      - ccc
    inputSchema:
      type: object
      required:
        - cik
      properties:
        cik:
          type: string
    outputSchema:
      type: object
      properties:
        tracking:
          type: string
        locator:
          type: string
        messages:
          type: string
        type:
          type: string
        content:
          type: string
        filerCCC:
          type: string
    underlyingCalls:
      - id: Generate CCC
        endpoint: POST /fm/{cik}/ccc
        purpose: Generate CCC
        params:
          cik: '{cik}'
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: list_delegation
    description: >-
      Task-oriented tool for delegation. Wraps 1 API endpoint(s) so a question is answered without
      multiple round-trips. Example question it answers: "View the delegations associated with the
      CIK.".
    evalCoverage:
      - q13
    entities:
      - delegation
    inputSchema:
      type: object
      required:
        - cik
      properties:
        cik:
          type: string
        max_items:
          type: integer
          description: Maximum records to return (caps pagination).
          default: 50
    outputSchema:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              tracking:
                type: string
              locator:
                type: string
              messages:
                type: string
              type:
                type: string
              content:
                type: string
              delegationsTo:
                type: string
              entityCik:
                type: string
              entityName:
                type: string
              status:
                type: string
              isRequest:
                type: string
              autoAddUsers:
                type: string
              lastUpdated:
                type: string
        count:
          type: integer
        truncated:
          type: boolean
          description: True if max_items capped the result.
    underlyingCalls:
      - id: View Delegations
        endpoint: GET /fm/{cik}/delegations
        purpose: View delegations
        params:
          cik: '{cik}'
    pagination:
      strategy: auto_paginate_until_limit
      maxPages: 5
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
  - name: create_delegation
    description: >-
      Task-oriented tool for delegation. Wraps 1 API endpoint(s) so a question is answered without
      multiple round-trips. Example question it answers: "Send a delegation invitation to another
      filer.".
    evalCoverage:
      - q14
    entities:
      - delegation
    inputSchema:
      type: object
      required:
        - cik
      properties:
        cik:
          type: string
    outputSchema:
      type: object
      properties:
        tracking:
          type: string
        locator:
          type: string
        messages:
          type: string
        type:
          type: string
        content:
          type: string
        filerInfo:
          type: string
        cik:
          type: string
        stateCode:
          type: string
        irsNumber:
          type: string
        entityStatus:
          type: string
        entityType:
          type: string
        dateIncorporated:
          type: string
    underlyingCalls:
      - id: Send Delegation Invitations
        endpoint: POST /fm/{cik}/delegations
        purpose: Send delegation invitations
        params:
          cik: '{cik}'
    rateLimiting:
      strategy: bounded_concurrency
      retry:
        onStatus:
          - 429
          - 503
        backoff: exponential
    errors:
      partialResults: false
      userVisibleMessages:
        auth_failed: Authentication failed. Check the configured credentials.
        rate_limited: The API rate limit was reached. Partial results may be returned.
        not_found: No matching records were found for the given inputs.
    reviewFlags: []
resources:
  - name: api_capability_index
    description: >-
      Normalized index of the 18 documented API endpoints (method, path, params, response fields)
      the tools are built from.
    uri: forge://capabilities
  - name: eval_coverage_matrix
    description: Maps each evaluation question to the tool that answers it, with confidence and gaps.
    uri: forge://coverage
prompts:
  - name: answer_with_api_context
    description: >-
      System/template prompt instructing the model to select the single best task-oriented tool,
      pass minimal inputs, and avoid issuing redundant multi-call sequences.
  - name: route_question_to_tool
    description: Few-shot template mapping representative eval questions to the tool that should handle them.
meta:
  generatedAt: '2026-06-22T23:05:33.853Z'
  generator: mcp-spec-forge
  endpointCount: 18
  questionCount: 14
  mode: deterministic
